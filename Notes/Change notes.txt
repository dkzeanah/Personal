Markdown file with artist to link

Artists new — function reads the md file, regexes the links into an array, and for loops the array to run link (just run)

something-exe added as a custom keyword

Win alt pgdn is align cursors
Win alt pgup adds cursors to line ends
The cursor trims are win alt arrows

ToolPeek — automatical tooltip timeout implementation

alt enter to select the down option is not needed because I have ctrl space for most things

alt 1 opens explorer and closes it

alt 2 opens the terminal and terminates it

alt 3 opens and closes the search and replace

now ctrl space and ^+space go up and down

every other alt number is shifted to the right

I made a clock :)
